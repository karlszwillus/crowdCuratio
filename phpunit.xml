<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="https://schema.phpunit.de/10.5/phpunit.xsd" bootstrap="vendor/autoload.php" colors="true" cacheDirectory=".phpunit.cache">
  <testsuites>
    <testsuite name="Unit">
      <directory suffix="Test.php">./tests/Unit</directory>
    </testsuite>
    <testsuite name="Feature">
      <directory suffix="Test.php">./tests/Feature</directory>
    </testsuite>
  </testsuites>
  <php>
    <server name="APP_ENV" value="testing"/>
    <server name="BCRYPT_ROUNDS" value="4"/>
    <server name="CACHE_DRIVER" value="array"/>
    <!--
            Pest läuft per Default gegen SQLite-in-memory, damit
            RefreshDatabase die Sail-Dev-MySQL NICHT durchrollt.
            Phase-2-Härtung (Phase 2 / C.9). Wer bewusst gegen MySQL
            testen will, überschreibt das per Env (DB_CONNECTION=mysql
            DB_DATABASE=… ./vendor/bin/sail pest).
        -->
    <server name="DB_CONNECTION" value="sqlite"/>
    <server name="DB_DATABASE" value=":memory:"/>
    <server name="MAIL_MAILER" value="array"/>
    <server name="QUEUE_CONNECTION" value="sync"/>
    <server name="SESSION_DRIVER" value="array"/>
    <server name="TELESCOPE_ENABLED" value="false"/>
  </php>
  <source>
    <include>
      <directory suffix=".php">./app</directory>
    </include>
    <exclude>
      <!--
        Console-Commands sind One-Off-Maintenance-Tools (FK-Audit,
        Media-Content-Schema-Audit, Migrations-Helper). Tests dafür
        sind disproportional teuer (DB-Schema-Inspektion, FK-Walks,
        explizite Data-Migrations) und bringen fachlich wenig
        Aussagekraft. 562 LOC bei 0 % verzerrten die Coverage-
        Bilanz konstant nach unten. Sollte künftig ein Command mit
        echter Domain-Logik dazukommen (z. B. ein Daily-Digest),
        diesen Exclude wieder aufweichen.

        Eingeführt in Phase 5a.V (Coverage-Welle, 2026-06-29).
      -->
      <directory>./app/Console/Commands</directory>
    </exclude>
  </source>
</phpunit>
